extends layout

block content
  script(src='/javascripts/batchAddCamper.js')
  h1 Add Camper

  #add 
    | Enter each camper on its own line, in "Last, First" format.  
    | hyphens (John Doe-Smith) and apostrophes (Barack O'Bama) are ok, but extra commas are not.
    form(name='addCamper', action='/setup/batchAddingCamper', method='POST', id='addCamperForm')
      label(for='campers') List of Campers
      textarea(name='campers', rows='15', cols='50') Wiggin, John
      br
      label(for='cabin') Cabin:
      select(name='cabin')
        - for(var i = 0; i < cabins.length; i++)
          option(value=cabins[i]) #{cabins[i]}

    .button#submit Add
