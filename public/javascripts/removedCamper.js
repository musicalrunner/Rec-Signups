$('document').ready( function() {

  // add 'undo functionality to the undo button
  var $undo = $('#undo');
  var camper = JSON.parse($('input').val());

  $undo.click(function () {
    $('form').submit();
  });









});
